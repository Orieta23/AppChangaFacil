<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    tools:context=".MainActivity">

    <!-- Contenedor principal con FAB flotante -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Contenido con barra de navegación inferior -->
        <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/white">

            <!-- Toolbar -->
            <androidx.appcompat.widget.Toolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="@color/blue_chamba"
                android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
                app:title="Chamba Fácil"
                app:titleTextColor="@android:color/white" />

            <!-- Fragmento de navegación -->
            <androidx.fragment.app.FragmentContainerView
                android:id="@+id/nav_host_fragment"
                android:name="androidx.navigation.fragment.NavHostFragment"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:background="@android:color/white"
                app:defaultNavHost="true"
                app:navGraph="@navigation/nav_graph" />
            <!-- El navGraph se cambia dinámicamente desde MainActivity -->

            <!-- Menú inferior -->
            <com.google.android.material.bottomnavigation.BottomNavigationView
                android:id="@+id/bottomNavigationView"
                android:layout_width="match_parent"
                android:layout_height="70dp"
                android:background="@android:color/white"
                android:paddingTop="10dp"
                android:paddingBottom="10dp"
                android:elevation="0dp"
                app:menu="@menu/menu_empleador"
                app:itemIconTint="@color/nav_item_color"
                app:itemTextColor="@color/nav_item_color" />
        </LinearLayout>

        <!-- FAB (solo para postulante, se oculta en empleador) -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fabBuscar"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:layout_gravity="bottom|center"
            android:layout_marginBottom="30dp"
            android:src="@drawable/ic_search"
            android:contentDescription="Buscar"
            app:backgroundTint="@android:color/white"
            app:tint="@android:color/black"
            android:elevation="12dp"
            android:translationZ="12dp"
            android:visibility="gone" />
        <!-- Lo ocultamos por defecto, se muestra solo en rol postulante -->
    </FrameLayout>

    <!-- Menú lateral (Drawer) -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@android:color/white"
        app:itemTextColor="@color/black"
        app:itemIconTint="@color/black"
        app:itemBackground="@drawable/nav_item_background"
        app:menu="@menu/nav_drawer_menu" />

</androidx.drawerlayout.widget.DrawerLayout>